name: CI

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v3
      
      - name: Say Hello
        run: echo "Hello from my Mac Studio! 🎉"
